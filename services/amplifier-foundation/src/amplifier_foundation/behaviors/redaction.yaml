behavior:
  name: behavior-redaction
  version: 1.0.0
  description: Redact secrets and PII from logs
  hooks:
  - class: amplifier_foundation.hooks.redaction.RedactionHook
    config:
      allowlist:
      - session_id
      - turn_id
      - span_id
      - parent_span_id
